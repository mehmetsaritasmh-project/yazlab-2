global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'fastapi_services'
    static_configs:
      - targets: ['dispatcher_service:8000'] 
      # Eğer diğer servislerde de metrik açarsan buraya ekleyebilirsin: 'user_service:8001' vb.